{
  "name": "bueno-docs",
  "version": "0.1.1",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/build.ts",
    "build": "bun run src/build.ts",
    "serve": "bun run src/serve.ts"
  },
  "dependencies": {
    "@buenojs/bueno": "^0.8.8"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.3.0"
  },
  "engines": {
    "bun": ">=1.3.0"
  }
}
